// js/supabase-config.js
// Paste your Supabase project credentials below to enable the cross-device
// backend. Both values come from: Supabase Studio → Project settings → API.
//
//   SUPABASE_URL      — "Project URL" (e.g., https://abcdefg.supabase.co)
//   SUPABASE_ANON_KEY — "anon public" key (safe to embed; RLS protects data)
//                       NOT the service-role key — that one stays secret.
//
// Leave both as empty strings to keep the app in local-only mode.

export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
